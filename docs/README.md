# docs

Drop a real screenshot of the brief-me menu-bar dropdown here as `screenshot.png`.
The top-level `README.md` references it (`![brief-me menu bar](docs/screenshot.png)`).

Suggested capture: open the menu-bar dropdown after a morning run (📬 with a couple of
action items and a few "rest" lines), then use **⌘⇧4 then Space** to grab just that window.
